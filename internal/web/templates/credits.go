package templates

// Attribution, kept in one place so the masthead and the about page cannot
// drift apart on who wrote this and where the code lives.
//
// itchgrep is GPL-3.0. Running a modified version over a network does not by
// itself oblige anyone to publish the changes - that is the AGPL, not the GPL -
// but the licence is only honoured in spirit if the source the site actually
// runs is reachable, so SourceURL must point at THIS fork rather than at
// upstream. UpstreamURL is credit, not a source offer: it is the original work
// this is derived from, and it no longer describes what is deployed here.
const (
	// SourceURL is the repository this deployment is built from. Update it if
	// the fork moves; a source link that 404s is worse than none.
	SourceURL = "https://github.com/SkilledAlpaca/itchgrep"

	// UpstreamURL and AuthorURL credit the original author, who wrote the
	// crawler, the index and the interface this is descended from.
	UpstreamURL = "https://github.com/wintermute-cell/itchgrep"
	AuthorURL   = "https://github.com/wintermute-cell"
	AuthorName  = "winterveil"
)
